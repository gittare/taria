cmake_minimum_required(VERSION 3.20)
project(taria_example_semantic_chunk CXX)

# This CMake file demonstrates how a C++ application would link against
# the generated Taria backend libraries to execute the compiled kernel.

find_package(CUDA REQUIRED)

# include_directories(../../../include)

# add_executable(run_semantic_chunk run.cpp)
# target_link_libraries(run_semantic_chunk PRIVATE TariaRuntime)
