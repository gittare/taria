cmake_minimum_required(VERSION 3.20)
project(taria_example_tensor_fusion CXX)

find_package(CUDA REQUIRED)

# add_executable(run_tensor_fusion run.cpp)
# target_link_libraries(run_tensor_fusion PRIVATE TariaRuntime)
