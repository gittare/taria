[workspace]
resolver = "2"
members = [
    "frontend",
    "ffi",
    "tools/tariac"
]

[profile.release]
lto = "thin"
opt-level = 3
debug = false
codegen-units = 1
panic = "abort" # Recommended for tighter FFI integration

[profile.dev]
opt-level = 0
debug = true
